"use strict"

// Convert the ternary below to a regular if statement in JavaScript
let price = 30;
let message = price >= 30 ? "This item is too expensive!" : "This item is affordable";

    function message(price) 
    if (price >= 30) {
        return "This item is too expensive"
    }
    else{
        return "This item is affordable"
    }
    

console.log(message);